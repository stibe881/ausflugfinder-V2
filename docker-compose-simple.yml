version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - CACHE_BUST=1
      target: production
    container_name: ausflug-manager-app
    ports:
      - "3000:3000"
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=mysql://admin2:!LeliBist.1561!@185.178.193.60:3306/ausflugfinder_v2
      - JWT_SECRET=aB3xY9mK2pQ8vL5nR7tW4sU6dF1gH0j9k2m5n8p0r3t5u7v9w1x3z5a7b9c1d3
      - VITE_APP_ID=ausflug-manager
      - VITE_APP_LOGO=
      - VITE_APP_TITLE=Ausflug Manager
      - VITE_ANALYTICS_ENDPOINT=
      - VITE_ANALYTICS_WEBSITE_ID=
      - VITE_OAUTH_PORTAL_URL=
    restart: unless-stopped
    networks:
      - ausflug-network

networks:
  ausflug-network:
    driver: bridge
