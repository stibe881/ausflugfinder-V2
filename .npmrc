# Configuration for npm (used by Ionic Appflow)
# pnpm is the primary package manager for local development (see packageManager in package.json)

# Allow legacy peer dependencies to resolve version conflicts
legacy-peer-deps=true

# Use same lockfile format as package-lock.json
package-lock=true
