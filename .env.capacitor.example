# Capacitor Environment Variables
# Kopiere diese Datei zu .env.capacitor und fülle die Werte aus

# ===== iOS (APNs) =====
# Wenn du echte APNs Push haben willst (optional)
# Sonst funktioniert Web Push auch in der iOS App

APPLE_PUSH_P8_KEY=<dein-apns-key-hier>
APPLE_TEAM_ID=<dein-apple-team-id>

# ===== Android (FCM) =====
# Firebase Project ID
FIREBASE_PROJECT_ID=ausflug-manager-xxx

# Firebase Private Key (aus google-services.json)
FIREBASE_PRIVATE_KEY=<dein-firebase-private-key>

# Firebase Client Email (aus google-services.json)
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@ausflug-manager.iam.gserviceaccount.com

# ===== App Settings =====
# Diese sind meist schon in package.json definiert, aber hier nochmal:

APP_ID=ch.ausflugfinder.app
APP_NAME=AusflugFinder
WEB_DIR=dist/public

# ===== Hinweise =====
# - APNs Setup ist optional. Web Push funktioniert auch in der iOS App!
# - FCM braucht nur Firebase Project ID, Private Key und Email
# - Diese Datei sollte NICHT in Git committed werden!
# - Nutze .env.local statt .env.capacitor in Produktion
